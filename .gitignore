.Rproj.user
.Rhistory
.RData
.Ruserdata
_book/
data
results
boxplot_files/
/data
/results
/figures

# NOTE: `figures` (bare) is intentionally NOT ignored — it would also match
# `docs/figures/`, the static images bookdown copies into the published site.
# The root port target is still ignored via `/figures` above.
/boxplot_files/
02_descriptive_statistics.Rmd

# Claude Code session artifacts
.claude/

# shinylive app data (derived from results/, regenerated by build_apps.R)
shiny-apps/*/data/
