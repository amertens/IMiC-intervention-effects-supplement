`r if (knitr::is_html_output()) '# Reproducibility {-}'`

This page records the R session that produced the current build of the supplement, so the exact package versions and platform are part of the public artefact.

```{r session-info, echo=FALSE}
sessionInfo()
```
